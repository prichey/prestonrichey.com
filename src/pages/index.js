import React from 'react';
import styled from 'styled-components';

const Index = styled.div`
  color: pink;
  font-size: 240px;
`;

export default () => (
  <Index>
    <div>Hello world!</div>
  </Index>
);
